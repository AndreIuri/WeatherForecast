name: Deploy React App to GitHub Pages

on:
  push:
    branches: ["main"]  # Change if using a different default branch
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18  # Adjust Node.js version if needed

      - name: Install dependencies
        run: npm install
        working-directory: Weather-React/Weather-React  # Change to your actual path

      - name: Build React App
        run: npm run build
        working-directory: Weather-React/Weather-React  # Change to your actual path

      - name: Disable Jekyll
        run: touch build/.nojekyll
        working-directory: Weather-React/Weather-React  # Change to your actual path

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: Weather-React/Weather-React/build  # Change to your actual build path

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
